# MOTU, 2021, on linux c++ version in Cmake file

needs CImg.h header ( included) and sqlite3.h 
A gamificated version of a pomodoro timer, with different stats that can be leveled up, 
a calendar, sound effects .
Unfortunately I was unable to compile and run the app in ints current state (which I did not change since last time it worked).
